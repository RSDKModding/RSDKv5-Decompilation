apply plugin: 'com.android.application'

android {
    ndkVersion "22.1.7171670"
    compileSdkVersion 29
    defaultConfig {
        applicationId "com.decomp.rsdkv5"
        minSdkVersion 23
        //minSdkVersion 30 // if you're getting framerate issues, uncomment this
        targetSdkVersion 29
        versionCode 1
        versionName "1.0"
        externalNativeBuild {
            ndkBuild {
                arguments "APP_PLATFORM=android-23"
                //arguments "APP_PLATFORM=android-30", "POST30=1" // if you're getting framerate issues, uncomment this
                abiFilters = []
                abiFilters.addAll(ABIFILTERS.split(';').collect{it as String})
            }
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt')
        }
    }
    if (!project.hasProperty('EXCLUDE_NATIVE_LIBS')) {
        sourceSets.main {
            jniLibs.srcDir 'libs'
        }
        externalNativeBuild {
            ndkBuild {
                path 'jni/Android.mk'
            }
        }
       
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
